<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="afterlog.css">
</head>
<style>
     .navi {
        margin: 0;
        top: 0;
        position: fixed;
        height: 70px;
        width: 100%;
        background-color: #00246b;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 20px;
    }
    .db {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        font-size: 18px;
        color: #adceed;
        text-decoration: none;
        margin: 10px;
        padding: 8px;
        border-radius: 10px;
        border: 2px solid;
    }
    .db:hover {
        font-size: 22px;
        color: white;
        transition: 0.15s ease-in;
        background-color: #3187b9;
        border-color: white;
    }
    .logo {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        font-size: 25px;
        color: #adceed;
        text-decoration: none;
        margin: 10px;
        padding: 6px;
        border: 2px solid white;
        text-align: center;
    }
    body {
        background-color: #7a94c3;
        margin: 0;
        padding: 0;
    }
    .db {
        margin-right: 40px;
    }
    /* Media Queries */
    @media only screen and (max-width: 811px) {
        .navi {
            flex-direction: column;
            height: auto;
            padding-bottom: 10px;
        }
        .logo {
            margin: 10px auto; 
            margin-left: 30px;
        }
        .home {
            margin-left: auto;
            margin-right: auto;
            margin-bottom: 10px; 
        }
        .db {
            font-size: 16px;
            margin: 5px;
            padding: 6px;
        }
        .body {
            margin-top: 150px; 
            margin-bottom: 50px; 
        }
        /* Hamburger Icon Styles */
        .hamburger-icon {
            display: block;
            position: absolute;
            top: 23px;
            right: 65px;
            width: 30px;
            height: 30px;
            cursor: pointer;
            z-index: 9999;
        }
        .hamburger-icon span {
            display: block;
            width: 30px;
            height: 3px;
            background: #adceed;
            margin-bottom: 5px;
        }
        .nav-links {
            display: none;
            position: absolute;
            top: 70px;
            left: 0;
            width: 100%;
            background-color: #00246b;
            z-index: 999;
        }
        .nav-links.active {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
    }
    .content {
        margin-top: 100px;
        display: flex;
        flex-direction: column; /* Stack items vertically */
        align-items: center; /* Center items horizontally */
    }
    .conta {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        font-size: 18px;
        color: #d2e3f4;
        text-decoration: none;
        margin: 10px;
        padding: 8px;
        border-radius: 10px;
        border: 2px solid;
        background-color: #0a307c;
    }
    .conta:hover {
        font-size: 22px;
        color: white;
        transition: 0.15s ease-in;
        background-color: #3187b9;
        border-color: white;
    }
    /* Responsive styles */
    @media only screen and (max-width: 768px) {
        .content {
            margin-top: 90px; /* Adjust margin for smaller screens */
        }
        .conta {
            font-size: 16px; /* Adjust font size for smaller screens */
            padding: 18px 12px; /* Adjust padding for smaller screens */
        }
    }
    h1 {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        font-weight: 600;
        background-color: #355aa6;
        padding: 10px;
        border-radius: 12px;
        border: 2px solid rgb(187, 236, 255);
        text-decoration: underline;
    }
    img {
        height: 70px;
        width: 80%;
        display: inline;
    }
    #images {
        display: flex;
        flex-direction: row;
    }
</style>
<body>
    <div class="navi">
        <div id="images">
            <img src="logo_mallareddy.png" alt="notfound">
            <img id="logo2" src="logo_121.png">
        </div>
        <div class="hamburger-icon" onclick="toggleMenu()">
            <span></span>
            <span></span>
            <span></span>
        </div>
        <div class="nav-links">
            <form id="logoutForm" action="/logout" method="POST">
                <button type="submit" class="db">Logout</button>
            </form>
        </div>
    </div>
    <div class="content">
        <h1>Admin Options:</h1>
        <form action="/adminoptions" method="POST">
        <button name="search" class="conta">Search Students</button>
        <button  name="attendance" class="conta">Check Attendance</button>
        <button href="#" class="conta">Read Data</button>
    </form>

    </div>
    
    
    <script>
        document.getElementById('logoutForm').addEventListener('submit', function() {
            setTimeout(function() {
                window.location.href = '/';
            }, 100);
        });
    </script>
    
    <script>
        function toggleMenu() {
            var navLinks = document.querySelector('.nav-links');
            navLinks.classList.toggle('active');
        }
    </script>
</body>
</html>
